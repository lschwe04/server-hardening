---
# Standard-Variablen für die Firewall-Rolle

# Standard-Policies
ufw_default_incoming: "deny"
ufw_default_outgoing: "allow"

# Zu öffnende Ports
ufw_allowed_ports:
  - 22    # SSH
  - 80    # HTTP
  - 443   # HTTPS